/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "img.spoonacular.com",
      "spoonacular.com",
      "placehold.co"
    ],
  },
};

export default nextConfig;
